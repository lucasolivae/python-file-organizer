# Python File Organizer

This is a simple Python script that automatically organizes files in a folder
by their type (images, videos, documents, etc).

## Features
- Organizes files by extension
- Creates folders automatically
- Easy to use

## How to use
1. Place the script in the folder you want to organize
2. Edit the `SOURCE_FOLDER` variable if needed
3. Run the script with:
